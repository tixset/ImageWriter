# 💾 ImageWriter

[![License: GPL v3](https://img.shields.io/badge/Лицензия-GPLv3-blue.svg)](https://www.gnu.org/licenses/gpl-3.0)
[![Platform](https://img.shields.io/badge/Платформа-Windows-0078d4.svg)](https://www.microsoft.com/windows)
[![Language](https://img.shields.io/badge/Язык-Delphi%207-ee1f35.svg)](https://www.embarcadero.com/products/delphi)
[![Version](https://img.shields.io/badge/версия-2.2.0-blue)](https://github.com/tixset/ImageWriter/releases)
[![GitHub](https://img.shields.io/badge/GitHub-tixset%2FImageWriter-181717.svg?logo=github)](https://github.com/tixset/ImageWriter)

[![GitHub Stars](https://img.shields.io/github/stars/tixset/ImageWriter?style=social)](https://github.com/tixset/ImageWriter/stargazers)
[![GitHub Forks](https://img.shields.io/github/forks/tixset/ImageWriter?style=social)](https://github.com/tixset/ImageWriter/network/members)
[![GitHub Issues](https://img.shields.io/github/issues/tixset/ImageWriter)](https://github.com/tixset/ImageWriter/issues)
[![Last Commit](https://img.shields.io/github/last-commit/tixset/ImageWriter)](https://github.com/tixset/ImageWriter/commits)
[![Code Size](https://img.shields.io/github/languages/code-size/tixset/ImageWriter)](https://github.com/tixset/ImageWriter)

[![English](https://img.shields.io/badge/lang-English-blue.svg)](README.md)
[![Russian](https://img.shields.io/badge/lang-Русский-red.svg)](README.ru.md)

*Утилита для записи образов дисков на физические устройства*

## 📸 Скриншот

![Главное окно — список проектов](../assets/screenshots/main-window.png)

---

## 📋 Обзор

ImageWriter — это мощный инструмент для записи образов дисков на физические накопители. Доступны две версии для различных сценариев использования и требований безопасности.

## Версии

### ImageWriter.exe (Классическая)
- **Требует права администратора** при запуске
- Монолитная архитектура приложения
- Подходит только для локальных файлов
- Простое и прямолинейное использование
- Повышенные привилегии на протяжении всего сеанса

### ImageWriterPro.exe (Рекомендуется)
- **Запускается без прав администратора** изначально
- Архитектура с разделением привилегий: GUI (контекст пользователя) + Worker (с повышением)
- **Поддержка сетевых дисков** - может читать образы с сетевых папок
- Запрос UAC **только при выполнении операций записи/чтения**
- Более безопасно - повышенные привилегии только когда необходимо

## Ключевые возможности

### Проверка хешей
- **MD5** - поддержка традиционного алгоритма хеширования
- **SHA-256** - современный криптографический хеш (соответствие FIPS 180-2)
- Выбор алгоритма через выпадающее меню
- Автоматическое создание и проверка .md5 / .sha256 файлов
- Режим проверки устройства - чтение и верификация содержимого диска

### Предпросмотр разделов
- **Автоматическое определение таблицы разделов** - поддержка MBR и GPT
- **Анализ перед записью** - показывает какие разделы будут созданы
- **Анализ архивированных образов** - чтение partition table напрямую из ZIP/GZIP/XZ/BZIP2/7z/tar.gz/tar.xz без полной распаковки
- **Эффективность памяти** - чтение только 64KB из архива для анализа (вместо GB)
- **Детальная информация** отображается перед записью:
  - Тип раздела (FAT32, NTFS, Linux ext4 и др.)
  - Размер в МБ и секторах
  - Начальный LBA адрес
  - Флаг загрузки (для MBR)
- Поддержка до 4 разделов MBR и 128 разделов GPT
- **Мгновенный предпросмотр** для архивов любого размера

### Поддержка сетевых дисков
ImageWriterPro может работать с файлами на сетевых дисках (подключенные диски, UNC пути) благодаря архитектуре с разделением привилегий. GUI работает в контексте пользователя и сохраняет доступ к сетевым ресурсам, в то время как только компонент записи на диск выполняется с повышением прав.

#### Авторизация на UNC-шаре
Если вы указываете UNC-путь (например, \\192.168.156.156\Workspace\file.img) и доступ к нему невозможен, программа автоматически вызывает стандартное окно Windows для ввода логина и пароля. После успешной авторизации операция продолжается с файлом на шаре. Это реализовано через модуль NetShareAuth и работает как для чтения, так и для записи образов на сетевые ресурсы.

### Поддержка архивов
- **7 форматов архивов** - ZIP, GZIP, XZ, BZIP2, 7z, tar.gz, tar.xz
- **Автоматическая распаковка** при записи на устройство
- **Потоковая распаковка** - без создания временных файлов
- **Анализ таблицы разделов из архивов** - просмотр MBR/GPT без извлечения всего образа
- **Эффективное чтение** - только 64KB для анализа GPT (vs GB полной распаковки)
- **Поддержка отмены** - безопасная отмена операций с архивами
- Динамическая загрузка zlib1.dll с механизмом резервных вариантов
- **Определение форматов** - автоматическое распознавание типа архива

### Интеграция с WMI
- Получение информации о дисках без прав администратора
- Отображение размеров и моделей дисков до повышения прав
- Улучшенный пользовательский опыт и прозрачность

### Функции безопасности
- **Защита от множественных экземпляров** - предотвращает запуск нескольких копий
- **Блокировка на основе мьютексов** - отдельные блокировки для GUI и Worker процессов
- **Определение типа диска** - различает сетевые, фиксированные и съемные диски
- **Автоматические расчеты** - размер блока и количество блоков вычисляются автоматически

### Производительность
- **Оптимизированный IPC** - буфер канала 65 КБ, блоки данных 4 КБ
- **Обновления прогресса** - каждые 256 КБ для минимизации накладных расходов
- **Фоновые операции** - неблокирующий интерфейс во время операций с диском

### Мониторинг здоровья устройств (Новое в 2.2.0+)
- **Автоматическая диагностика** при выборе устройства:
  - **Информация WMI**: ёмкость, серийный номер, размер сектора
  - **SMART данные**: температура, переназначенные сектора, часы работы
  - **Валидация разделов**: целостность GPT/MBR таблиц с использованием точных IOCTL размеров дисков
- **Визуальные индикаторы здоровья**:
  - Owner-draw ComboBox с цветными фонами (зелёный/оранжевый/красный)
  - Иконки статуса: ✅ (здоров), ⚠️ (предупреждение), ❌ (критично), ❔ (неизвестно)
  - Всплывающие подсказки с кратким описанием состояния
- **Кнопка Health Report** - детальный анализ:
  - Аппаратная диагностика, SMART метрики, валидация разделов
  - Анализ трендов и обнаружение деградации
  - Обнаружение низкой скорости (порог 5 МБ/с)
- **Безопасность операций записи**:
  - Блокирует запись на устройства со статусом CRITICAL
  - Предупреждает перед записью на устройства WARNING
  - Предотвращает потерю данных на отказывающем оборудовании
- **Исправленные критические ошибки**:
  - Исправление COM reentrancy через PostMessage(WM_REFRESH_DEVICES)
  - Коррекция логики повторов WMI (5 попыток × 500мс)
  - Устранены ложные ошибки валидации разделов (IOCTL vs WMI размеры)
- **Документация**: См. `docs/DEVICE_HEALTH_FEATURES.md` для полного руководства

## Системные требования

- **Операционная система**: Windows XP / Vista / 7 / 8 / 10 / 11
- **Компилятор**: Delphi 7 (для сборки из исходников)
- **Среда выполнения**: Дополнительные зависимости не требуются
- **Привилегии**: Права администратора нужны только для операций записи/чтения

## Сборка из исходников

### Использование build.bat (Рекомендуется)
```cmd
build.bat
```

Это выполнит:
- Очистку временных файлов (*.dcu, *.ddp, *.RES)
- Компиляцию ImageWriter.exe и ImageWriterPro.exe
- Отображение сообщений об успехе/ошибках

### Ручная компиляция
```cmd
DCC32.EXE -B -U"studio;studio\md5;studio\random" ImageWriter.dpr
DCC32.EXE -B -U"studio;studio\md5;studio\random" ImageWriterPro.dpr
```

## Архитектура

### Архитектура ImageWriterPro

```
┌─────────────────────────────────────────────────────────────┐
│              GUI процесс (Контекст пользователя)             │
│  - Пользовательский интерфейс (VCL формы)                    │
│  - Файловые операции (чтение с сетевых/локальных дисков)     │
│  - Доступ к сетевым дискам сохранён                          │
│  - Манифест: asInvoker (повышение прав не требуется)         │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ IPC через Named Pipes
                        │ (8 типов сообщений: Data, Log, Progress,
                        │  Completed, Error, Cancel, Ready)
                        │
┌───────────────────────▼─────────────────────────────────────┐
│              Worker процесс (Контекст администратора)        │
│  - Доступ к физическим дискам (\\?\PhysicalDrive#)           │
│  - IOCTL операции                                            │
│  - Запись в сырые секторы                                    │
│  - Запускается через ShellExecuteEx с "runas"                │
│  - Запрос UAC отображается при необходимости                 │
└─────────────────────────────────────────────────────────────┘
```

### Протокол коммуникации

Протокол IPC использует 8 типов сообщений:

1. **ipcData** - Блоки данных файла (куски по 4 КБ)
2. **ipcLog** - Сообщения логов от worker к GUI
3. **ipcProgress** - Обновления прогресса (обработано байт)
4. **ipcCompleted** - Операция успешно завершена
5. **ipcError** - Произошла ошибка во время операции
6. **ipcCancel** - Запрос отмены операции
7. **ipcReady** - Worker готов получать данные
8. **ipcControl** - Управляющие сообщения (информация о размере)

### Протокол синхронизации

1. GUI запускает worker процесс с параметром `--worker`
2. Worker создаёт сервер Named Pipe и ждёт подключения
3. GUI подключается к каналу
4. Worker отправляет **ipcReady** после открытия физического устройства
5. GUI ждёт **ipcReady**, обрабатывает ожидающие сообщения
6. GUI отправляет общий размер, затем начинает отправку блоков данных
7. Worker получает и записывает блоки пока `TotalProcessed >= TotalSize`
8. Worker отправляет **ipcCompleted** по завершении
9. GUI продолжает читать буферизованные сообщения до завершения

## Структура проекта

```
ImageWriter/
├── ImageWriter.dpr          # Главный проект (requireAdministrator)
├── ImageWriterPro.dpr       # Pro версия с разделением привилегий
├── mainform.pas/dfm         # Главная форма GUI (общая для обеих версий)
│
├── Основные модули (архитектура ImageWriterPro)
│   ├── ElevationHelper.pas  # Повышение прав UAC и проверки привилегий
│   ├── IPCPipe.pas          # Реализация IPC через Named Pipes
│   ├── WorkerMode.pas       # Логика worker процесса
│   └── BinFile.pas          # Кроссплатформенная абстракция доступа к дискам
│
├── Платформенно-зависимые
│   ├── WinBinFile.pas       # Доступ к физическим дискам Windows
│   ├── UnixBinFile.pas      # Доступ к файлам Unix/Linux (legacy)
│   ├── WinIOCTL.pas         # Определения Windows IOCTL
│   └── Native.pas           # Обёртки Native API
│
├── Поддержка архивов
│   ├── ExtractZLib.pas      # Извлечение zlib1.dll из ресурсов
│   ├── GZipStream.pas       # Обработка архивов GZIP/ZIP
│   └── Zlib.pas             # Обёртка ZLib
│
├── Утилиты
│   ├── DiskIO.pas           # Старый код дискового ввода-вывода
│   ├── volume.pas           # Операции с томами
│   └── QTThunkU.pas         # Старый код thunking
│
├── Ресурсы
│   ├── dd.rc                # Ресурсы ImageWriter
│   ├── resources_pro.rc     # Ресурсы ImageWriterPro
│   ├── dd.exe.manifest      # Манифест ImageWriter (requireAdministrator)
│   └── ImageWriterPro.exe.manifest  # Манифест Pro (asInvoker)
│
├── Библиотеки Studio
│   └── studio/
│       ├── debug.pas        # Утилиты отладки (УСТАРЕЛО - перенаправляет в LogUtils)
│       ├── persrc.pas       # Постоянные ресурсы
│       ├── studio_tools.pas # Общие инструменты
│       ├── md5/md5.pas      # MD5 хеш
│       └── random/MT19937.pas  # Генератор случайных чисел Mersenne Twister
│
├── Документация
│   ├── README.md            # Документация на английском
│   ├── README.ru.md         # Этот файл (русский)
│   ├── CODING_STYLE_GUIDE.md # Руководство по стилю кода
│   ├── COMMIT_CONVENTIONS.md # Стандарты Git коммитов
│   ├── BUILD_WITH_ZLIB.txt  # Инструкции по сборке с ZLib
│   └── STREAMING_GZIP_RU.md # Заметки о потоковой обработке GZIP
│
├── Конфигурация
│   ├── BUILD_CONFIGURATION.md  # Инструкции по сборке
│   ├── *.dof                   # Настройки проекта IDE
│   └── *.cfg                   # Конфигурация компилятора
│
└── Скрипты сборки
    ├── build.bat            # Автоматизированный скрипт сборки
    └── .gitignore           # Правила игнорирования Git
```

## Использование

### ImageWriter.exe (Классическая версия)

1. Запустите от имени администратора (запрос UAC при запуске)
2. Выберите входной файл (образ, .gz, .zip)
3. Выберите выходное устройство (физический диск или файл)
4. Нажмите "Write" или "Read"
5. Дождитесь завершения

### ImageWriterPro.exe (Рекомендуется)

1. Запустите обычным образом (без запроса UAC)
2. Просмотрите и выберите входной файл (поддерживаются сетевые диски)
3. Выберите выходное устройство из выпадающего списка
4. Нажмите "Write" или "Read"
5. **Появится запрос UAC** для повышения прав
6. Worker процесс запускается и выполняет операцию
7. Прогресс отображается в GUI
8. Worker автоматически закрывается по завершении или при закрытии GUI

## Технические детали

### Совместимость с Delphi 7

- Использует `array of Byte` вместо `TBytes` (в Delphi 7 нет TBytes)
- Модуль ActiveX для `CoCreateGuid` (генерация GUID)
- Пользовательская функция `CtlCode()` для констант IOCTL
- UI на основе VCL (не FireMonkey)

### Реализация IPC

- **Named Pipes**: `\\.\pipe\ImageWriterPro_[GUID]`
- **Размер буфера**: 65536 байт
- **Блоки данных**: Куски по 4 КБ для оптимальной производительности
- **Тайм-аут**: 5 секунд для подключения, бесконечно для операций
- **Двунаправленный**: Полнодуплексная коммуникация

### Интеграция с WMI

Использует WMI (Windows Management Instrumentation) для запроса информации о дисках без повышения прав:
- `Win32_DiskDrive` - Получение размера диска, модели, серийного номера
- `Win32_DiskPartition` - Информация о разделах
- Откат к IOCTL когда WMI недоступен

### Защита от множественных экземпляров

Глобальные мьютексы обеспечивают запуск только одного экземпляра:
- **Мьютекс GUI**: `Global\ImageWriterPro_GUI_Mutex`
- **Мьютекс Worker**: `Global\ImageWriterPro_Worker_[PipeName]`

Каждый экземпляр worker имеет свой мьютекс на основе имени канала (GUID).

## Известные ограничения

1. **Операция READ ещё не использует потоковую передачу IPC**
   - Worker создаёт файл напрямую в своём контексте
   - Не может сохранять на сетевые диски при повышенных правах
   - Обходной путь: Используйте локальный путь, затем скопируйте вручную

2. **Блокировка/разблокировка томов не делегирована worker**
   - GUI пытается заблокировать тома без повышения прав
   - Может не сработать на системных дисках
   - Не влияет на успех операции записи

## Лицензия

GPL (см. исходный код для деталей)

## История версий

### 2.2.0 (2025-12-22) - Унификация системы логирования

- **Унифицированная система логирования** - debug.pas теперь перенаправляет вызовы в LogUtils.pas (wrapper подход)
- **Улучшенное качество кода** - Добавлены предупреждения об устаревании legacy debug.pas
- **Полная обратная совместимость** - Весь существующий код работает без изменений
- **Расширенная документация** - Добавлено руководство LOGGING_MIGRATION_GUIDE.md

### 2.1.0 (2025-12-18) - Улучшенный пользовательский опыт

- **Улучшенное логирование** - Расширенная информация при запуске (путь программы, версия, редакция, имя ПК, пользователь)
- **Исправлено перечисление устройств** - Корректное отображение volLink через GetDiskExtents
- **Определение размера дисков** - Резервный метод GetDiskExtents при сбое DriveTotalBytes
- **Управление ресурсами** - Восстановлены иконки приложения и информация о версии
- **Структура проекта** - Переименован dd.dpr в ImageWriterPro.dpr для ясности
- **Автоматический выбор диалога** - Использует UserContext диалог при повышении на Vista+, откат к VCL на XP

### 2.0.0 (2025-12-18) - Релиз с разделением привилегий
- Реализована архитектура с разделением привилегий
- Добавлена поддержка сетевых дисков через потоковую передачу IPC
- Интеграция WMI для информации об устройствах без повышения прав
- Исправлены проблемы с deadlock в IPC (протокол синхронизации ipcReady)
- Добавлен автоматический расчёт блоков при вводе пути
- Защита от множественных экземпляров с мьютексами
- Оптимизированы обновления прогресса (каждые 256 КБ вместо 4 КБ)
- Исправлена проблема "99% завершения" (условие выхода на основе размера)
- Добавлено определение типа диска (Сетевой/Фиксированный/Съёмный)
- Worker автоматически закрывается при выходе из GUI
- Создана комплексная документация и автоматизация сборки

## Вклад в проект

Вклады приветствуются! Пожалуйста, убедитесь что:
- Код компилируется с Delphi 7
- Оба ImageWriter.exe и ImageWriterPro.exe собираются успешно
- Протестировано с локальными и сетевыми дисками
- Документация обновлена для значительных изменений

## Документация

- **[Руководство по стилю кодирования](CODING_STYLE_GUIDE.md)** - Полное руководство по стандартам кодирования
- **[Конвенции коммитов](COMMIT_CONVENTIONS.md)** - Руководство по Git коммитам
- **[README (English)](README.md)** - Документация на английском
- **[README (Русский)](README.ru.md)** - Документация на русском (этот файл)

### Дорожная карта развития

**Текущий статус:** Версия 2.2.0 (выпущена 22 декабря 2025)
- ✅ Интерфейс командной строки (CLI) - ЗАВЕРШЕНО
- ✅ Поддержка форматов архивов (7 форматов) - ЗАВЕРШЕНО
- ✅ Анализ таблиц разделов - ЗАВЕРШЕНО
- ✅ Улучшения безопасности и валидации - ЗАВЕРШЕНО
- ✅ Инфраструктура автоматического тестирования - ЗАВЕРШЕНО
- 🔜 Будущие улучшения отслеживаются в ROADMAP.md

См. [ROADMAP.md](../ROADMAP.md) для подробных планов развития и будущих функций.

## Авторство

Оригинальный проект ImageWriter с дополнениями:
- Архитектура с разделением привилегий
- Слой IPC коммуникации
- Интеграция WMI
- Поддержка сетевых дисков

## Поддержка

По вопросам и проблемам, пожалуйста, проверьте:
1. Этот README и [README.md](README.md) (английская версия)
2. [CHANGELOG.md](../CHANGELOG.md) для истории версий
3. [ROADMAP.md](../ROADMAP.md) для планируемых функций
4. Комментарии в исходном коде и история Git
5. [GitHub Issues](https://github.com/tixset/ImageWriter/issues) для известных проблем

## Примеры использования

### Запись образа Ubuntu на USB флешку

**ImageWriter.exe:**
```
1. Запустить ImageWriter.exe (запрос UAC)
2. Input: C:\Downloads\ubuntu-24.04-desktop-amd64.iso
3. Output: \\?\PhysicalDrive1 (USB флешка)
4. Нажать Write
5. Дождаться завершения
```

**ImageWriterPro.exe:**
```
1. Запустить ImageWriterPro.exe (без UAC)
2. Input: \\server\share\ubuntu-24.04-desktop-amd64.iso (сетевой диск!)
3. Output: \\?\PhysicalDrive1 (USB флешка)
4. Нажать Write (теперь запрос UAC)
5. Worker запускается и пишет данные
6. Прогресс отображается в реальном времени
7. Worker закрывается автоматически
```

### Создание резервной копии SD карты

```
1. Вставить SD карту
2. Запустить ImageWriterPro.exe
3. Output: C:\Backups\sdcard.img
4. Input: \\?\PhysicalDrive2 (SD карта)
5. Нажать Read (запрос UAC)
6. Образ сохраняется локально
```

**Примечание**: Для сохранения на сетевой диск используйте ImageWriter.exe или сначала сохраните локально, затем скопируйте.

## Устранение неполадок

### Проблема: "Не удаётся открыть устройство"
**Решение**: 
- Убедитесь что устройство не используется (размонтируйте в Управлении дисками)
- Закройте Проводник и другие программы, обращающиеся к устройству
- Используйте ImageWriter.exe если ImageWriterPro не работает

### Проблема: "Доступ запрещён" при записи
**Решение**:
- Подтвердите запрос UAC
- Проверьте что диск не защищён от записи физически
- Убедитесь что антивирус не блокирует операцию

### Проблема: "Не вижу сетевой диск в списке"
**Решение**:
- Используйте ImageWriterPro.exe (не ImageWriter.exe)
- Введите UNC путь вручную: \\server\share\file.iso
- Убедитесь что сетевой диск подключен в контексте пользователя

### Проблема: Worker не запускается
**Решение**:
- Проверьте что другой экземпляр не запущен (мьютекс блокирует)
- Посмотрите логи в окне GUI
- Убедитесь что UAC включён в системе

## Технические подробности реализации

### Почему две версии?

**Проблема UAC и сетевых дисков:**
Когда процесс запускается с повышенными правами (requireAdministrator), Windows изолирует его от сетевых ресурсов пользователя по соображениям безопасности. Это означает что:
- Подключенные сетевые диски (Z:, Y: и т.д.) **невидимы**
- UNC пути (\\server\share) могут **не работать** с учётными данными пользователя
- Процесс работает в контексте администратора, не пользователя

**Решение - Разделение привилегий:**
ImageWriterPro использует два процесса:
1. **GUI процесс** (пользователь) - видит все сетевые диски, читает файлы
2. **Worker процесс** (администратор) - пишет только на физический диск

Данные передаются между процессами через Named Pipes.

### Как работает IPC?

```pascal
// GUI процесс:
1. Открывает файл (может быть на сетевом диске)
2. Запускает worker с параметром --worker и именем канала
3. Подключается к Named Pipe
4. Ждёт сигнал ipcReady от worker
5. Отправляет размер файла
6. Читает файл блоками по 4 КБ
7. Отправляет блоки через IPC
8. Обновляет прогресс в UI
9. Отправляет ipcCompleted
10. Закрывает канал

// Worker процесс:
1. Получает имя канала из параметров командной строки
2. Создаёт Named Pipe сервер
3. Ждёт подключения GUI
4. Открывает физический диск (\\?\PhysicalDrive#)
5. Отправляет ipcReady
6. Получает размер данных
7. Получает блоки данных через IPC
8. Пишет блоки на физический диск
9. Продолжает до TotalProcessed >= TotalSize
10. Отправляет ipcCompleted
11. Закрывается
```

### Оптимизации производительности

1. **Размер блока 4 КБ**: Баланс между накладными расходами IPC и задержкой
2. **Буфер канала 65 КБ**: Достаточно для нескольких блоков, предотвращает deadlock
3. **Прогресс каждые 256 КБ**: Уменьшает вызовы Synchronize (переключение потоков дорого)
4. **Асинхронное чтение**: GUI читает следующий блок пока worker пишет текущий
5. **Без временных файлов**: GZIP/ZIP распаковываются на лету

### Безопасность

1. **Минимальные привилегии**: Повышение только когда нужно (Write/Read)
2. **Изоляция процессов**: Сбой worker не роняет GUI
3. **Проверка прав**: IsProcessElevated() перед операциями
4. **GUID для каналов**: Предотвращает конфликты имён и атаки
5. **Мьютексы**: Защита от race conditions и множественных экземпляров

## FAQ

**Q: Какую версию использовать?**
A: ImageWriterPro.exe для повседневного использования, особенно с сетевыми дисками. ImageWriter.exe для простоты или совместимости.

**Q: Безопасно ли запускать от администратора?**
A: Да, но ImageWriterPro безопаснее т.к. повышает права только когда нужно.

**Q: Можно ли записать образ с сетевого диска?**
A: Да, но только с ImageWriterPro.exe. ImageWriter.exe не увидит сетевые диски.

**Q: Поддерживаются ли сжатые образы?**
A: Да, ZIP и GZIP распаковываются автоматически на лету.

**Q: Нужны ли дополнительные DLL?**
A: Нет, zlib1.dll встроена в ресурсы и извлекается автоматически.

**Q: Можно ли запустить несколько копий?**
A: Нет, мьютексы предотвращают множественные экземпляры для безопасности.

**Q: Работает ли на Windows 11?**
A: Да, тестировалось на Windows XP/7/10/11.

**Q: Можно ли собрать с современным Delphi?**
A: Возможно с изменениями (TBytes, новые RTL функции), но проект для Delphi 7.
